{
  "name": "budget-allocation-demo",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Distributes an additional budget proportionally across employees in multiple currencies.",
  "engines": { "node": ">=20" },
  "scripts": {
    "start": "node tools/serve.mjs",
    "test": "node --test tests/*.test.js",
    "generate": "python3 tools/generate_employees.py"
  }
}
